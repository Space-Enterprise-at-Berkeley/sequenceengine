[build-system]
requires = [
    "setuptools",
    "setuptools-scm"
]
build-backend = "setuptools.build_meta"

[project]
name = "sequenceengine"
requires-python = ">=3.9"
dynamic = ["version"]

[tool.setuptools_scm]

[tool.pyright]
typeCheckingMode = "strict"
